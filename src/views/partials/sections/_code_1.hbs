{{! prettier-ignore }}
<pre><code class='line-numbers language-javascript'>import FP from '@fingerprintjs/fingerprintjs-pro';

FP.load({ token })
  .then(client => fp.get())
  .then(res => {
    console.log(res.visitorId);
  });
</code></pre>