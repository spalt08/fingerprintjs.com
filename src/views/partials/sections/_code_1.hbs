{{! prettier-ignore }}
<pre><code class='line-numbers language-javascript'>import FP from '@fingerprintjs/fingerprintjs';

FP.load()
  .then(client => fp.get({ token }))
  .then(res => {
    console.log(res.visitorId);
    console.log(res.botProbability);
  });
</code></pre>